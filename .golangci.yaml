version: "2"

run:
  timeout: 5m
  tests: true
  modules-download-mode: readonly

linters:
  default: all
  disable:
    - depguard
    - wsl
    - wsl_v5
    - paralleltest
    - nlreturn
    - perfsprint
    - noinlineerr
    - tagliatelle
    - varnamelen
    - testpackage
    - exhaustruct
    - gochecknoglobals
    - err113
    - wrapcheck
    - mnd
    - noctx
  settings:
    gosec:
      excludes:
        - G703 # Path traversal via taint analysis: CLI tool intentionally accepts user-specified paths
        - G705 # XSS via taint analysis: CLI stdout output, XSS not applicable
  exclusions:
    rules:
      - path: '(.+)_test\.go'
        linters:
          - gocyclo
          - cyclop
          - gocognit
          - funlen
          - maintidx
          - goconst

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/masawada/mdp
